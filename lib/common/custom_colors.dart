import 'package:flutter/material.dart';

class CustomColors {
  String id;
  Color color;
  bool isSelected;

  CustomColors(
      {required this.id, required this.color, this.isSelected = false});
}
